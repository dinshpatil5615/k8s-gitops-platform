Self Service Deployment Platform

In real companies developers struggle with kubernetes complexity. I built a small internal developer platform which allowes developers to deploy application using Git push while the platform handles ci/cd deployment ingress and monitoring automatically where developer does not touch core kubernetes directly
